<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Aperçu des Informations du Candidat</title>
		<style>
			body,
			html {
				font-family: 'Montserrat', sans-serif;
				font-size: 14px; /* Increased from 12px */
				margin: 0;
				padding: 0;
			}
			.container {
				max-width: 700px; /* Reduced from 800px */
				margin: 0 auto;
				padding: 20px; /* Increased from 10px */
				box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
			}
			.timbre-fiscal {
				position: absolute;
				top: 30px;
				left: 30px;
				border: 2px solid #000;
				padding: 10px;
				font-weight: bold;
				font-size: 16px;
			}
			.header {
				position: relative;
				height: 120px;
				margin-bottom: 30px;
				margin-top: 50px;
			}
			.flag {
				position: absolute;
				top: 40px;
				height: 50px;
			}
			.logo {
				position: absolute;
				top: 20px;
				left: 50%;
				transform: translateX(-50%);
				height: 100px;
			}
			.main-content {
				border: 2px solid #0e6d4d;
				border-radius: 10px;
				padding: 30px;
				margin-top: 30px;
			}
			.title {
				text-align: center;
				margin-bottom: 30px; /* Increased from 10px */
			}
			.title h2 {
				font-size: 12px; /* Increased from 12px */
				margin: 0;
			}
			.title h3 {
				font-size: 20px; /* Increased from 16px */
				margin: 5px 0; /* Increased from 5px 0 */
			}
			.info-section {
				display: flex;
				justify-content: space-between;
				align-items: flex-start; /* Align items to the top */
				margin-top: 40px; /* Increased margin-top */
				margin-bottom: 40px; /* Added margin-bottom */
			}
			.info-column {
				flex: 1;
				margin-right: 20px;
			}
			.info-column:last-child {
				margin-right: 0;
			}
			.info-columns {
				display: inline-block;
				justify-content: space-between;
			}
			.info-item {
				margin-bottom: 10px; /* Increased from 5px */
			}
			.info-label {
				font-size: 11px;
				font-style: italic;
				border-bottom: 1px solid #111827;
				display: inline-block;
				width: 100%;
				margin-bottom: 5px;
			}
			.info-label1 {
				font-size: 11px;
				font-style: italic;
				border-bottom: 1px solid #111827;
				display: inline-block;
				width: 60%;
				margin-bottom: 5px;
			}
			.info-value {
				font-weight: 600;
				display: block;
			}

			.info-table {
				width: 100%;
				border-collapse: separate;
				border-spacing: 0 10px;
			}
			.info-table td {
				vertical-align: top;
				padding: 0;
			}
			.photo-placeholder {
				position: absolute;
				top: 335px;
				right: 80px;
				height: 130px;
				width: 150px;
				border: 1px solid #D1D5DB;
				background-color: #D1D5DB;
				/* display: flex;
            justify-content: right;
            align-items: right;
            margin-left: 20px; /* Space between info and photo */
				/* flex-shrink: 0; Prevents shrinking */ */
			}
			.concours-info {
				margin-top: 40px; /* Increased margin-top */
				margin-bottom: 40px; /* Added margin-bottom */
			}
			.concours-header {
				background-color: #0e6d4d;
				color: white;
				padding: 10px; /* Increased from 5px */
				text-align: center;
			}
			.concours-content {
				border: 1px solid #10B981;
				padding: 15px;
			}
			.signature-section {
				margin-top: 40px;
				text-align: right;
			}
			.signature-box {
				width: 150px;
				height: 80px;
				border: 1px solid #000;
				margin: 10px 0 0 auto;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="timbre-fiscal">Timbre Fiscal</div>
			<div class="header">
				<img src="{{ absolute_url(asset('images/cameroon.png')) }}" alt="Cameroon Flag" class="flag" style="left: 20px;">
				<img src="{{ absolute_url(asset('images/uiecc_logo.png')) }}" alt="UIECC Logo" class="logo">
				<img src="{{ absolute_url(asset('images/congo.png')) }}" alt="Congo Flag" class="flag" style="right: 20px;">
			</div>

			<div class="main-content">
				<div class="title">
					<h2>UNIVERSITE INTER-ETATS CAMEROUN-CONGO</h2>
					<h3>RECIPISSE DE DEPOT DE DOSSIER</h3>
				</div>

				<div class="info-section relative">

					<div class="info-column">
						<div class="info-item">
							<span class="info-label1">No Dossier:</span>
							<span class="info-value" style="color: #DC2626;">***************</span>
						</div>
						<div class="info-item">
							<span class="info-label1">Filiere choisie:</span>
							<span class="info-value">{{ candidate.field }}</span>
						</div>
						<div class="info-item">
							<span class="info-label1">Session du concours:</span>
							<span class="info-value">2024</span>
						</div>
					</div>
					<div class="photo-placeholder">
						{% if candidate.photo %}
							<img src="{{ absolute_url(asset('uploads/photos/' ~ candidate.photo)) }}" alt="Photo du candidat" style="width: 100%; height: 100%; object-fit: cover; border-radius: inherit;">
						{% else %}
							<div class="rounded-full h-32 w-32 bg-gray-300 flex items-center justify-center text-gray-500">
								<span style="font-size: 24px;">?</span>
							</div>
						{% endif %}
					</div>

				</div>


				<div class="concours-info">
					<div class="concours-header">
						<h4 style="margin: 0;">INFORMATIONS SUR LE CONCOURS</h4>
					</div>
					<div class="concours-content">
						<table class="info-table">
							<tr>
								<td width="50%">
									<span class="info-label">Nom et Prenoms:</span>
									<span class="info-value">{{ candidate.name }}</span>
								</td>
								<td width="50%">
									<span class="info-label">Sexe:</span>
									<span class="info-value">{{ candidate.sexe }}</span>
								</td>
							</tr>
							<tr>
								<td>
									<span class="info-label">Date de naissance:</span>
									<span class="info-value">{{ candidate.dateOfBirth|date('d/m/Y') }}</span>
								</td>
								<td>
									<span class="info-label">Lieu de naissance:</span>
									<span class="info-value">{{ candidate.placeOfBirth }}</span>
								</td>
							</tr>
							<tr>
								<td>
									<span class="info-label">Nationalité:</span>
									<span class="info-value">{{ candidate.nationality }}</span>
								</td>
								<td>
									<span class="info-label">Diplome presente | Serie:</span>
									<span class="info-value">{{ candidate.certificate }}</span>
								</td>
							</tr>
							<tr>
								<td>
									<span class="info-label">Centre d'Examen:</span>
									<span class="info-value">{{ candidate.examinationCenter }}</span>
								</td>
								<td></td>
							</tr>
							<tr>
								<td>
									<span class="info-label">Langue de Composition:</span>
									<span class="info-value">{{ candidate.language }}</span>
								</td>
								<td></td>
							</tr>
						</table>
					</div>
				</div>

				<div class="signature-section">
					<span style="font-weight: 600; font-size: 10px;">SIGNATURE DU RECEPTEUR</span>
					<div class="signature-box"></div>
					<div style="font-size: 10px; font-style: italic; margin-top: 30px;">
						Imprimee le
						{{ "now"|date("d F Y, h:i a") }}
					</div>
				</div>
			</div>
		</body>
	</body>
</html></div></body></html>

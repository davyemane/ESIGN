{% extends 'base.html.twig' %}

{% block head %}
    {% block title %}
        {{ 'registration_page.title'|trans }}
    {% endblock %}
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-white p-8 mt-10">
        <div class="container max-w-7xl bg-white rounded-xl shadow-2xl overflow-hidden">
            <div class="flex flex-col md:flex-row">
                <!-- Left section with logo and text -->
                <div class="w-full md:w-1/2 p-8 flex flex-col justify-center items-center bg-gradient-to-br from-blue-100 to-green-100">
                    <img class="h-64 w-auto light-mode-logo" src="{{ asset('/images/uiecc_logo.png') }}" alt="{{ 'registration_page.logo_alt'|trans }}">
                    <img class="h-64 w-auto dark-mode-logo hidden" src="{{ asset('/images/uiecc_logo_dark.svg') }}" alt="{{ 'registration_page.logo_alt'|trans }}">
                    <h1 class="mt-8 text-3xl font-bold text-center">{{ 'registration_page.welcome_message'|trans }}</h1>
                    <p class="mt-4 text-center text-lg">{{ 'registration_page.slogan'|trans }}</p>
                </div>

                <!-- Right section with registration form -->
                <div class="w-full md:w-1/2 p-32">
                    <div class="flex flex-col">
                        <h2 class="text-4xl font-bold text-center mb-2">{{ 'registration_page.heading'|trans }}</h2>
                        <span class="text-sm font-normal mb-10 text-center">{{ 'registration_page.description'|trans }}</span>
                    </div>
                    
                    {{ form_start(registrationForm, {'attr': {'class': 'space-y-6'}}) }}
                        <div>
                            <label for="" class="block text-sm font-semibold">{{'registration_page.username_label'|trans}}</label>
                            {{ form_label(registrationForm.username, ' ', {'label_attr': {'class': 'block text-sm font-semibold'}}) }}
                            {{ form_widget(registrationForm.username, {'attr': {'class': 'mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary'}}) }}
                        </div>

                        <div>
                            <label for="" class="block text-sm font-semibold">{{'registration_page.password'|trans}}</label>
                            {{ form_label(registrationForm.plainPassword.first, ' ', {'label_attr': {'class': 'block text-sm font-semibold'}}) }}
                            <div class="mt-1 relative">
                                {{ form_widget(registrationForm.plainPassword.first, {'attr': {'class': 'block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary'}}) }}
                            </div>
                        </div>

                        <div class="">
                            <label for="" class="block text-sm font-semibold">{{'registration_page.confirm_password'|trans}}</label>
                            {{ form_label(registrationForm.plainPassword.second,' ', {'label_attr': {'class': 'block text-sm font-semibold'}}) }}
                            <div class="mt-1 relative">
                                {{ form_widget(registrationForm.plainPassword.second, {'attr': {'class': 'block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary mb-10'}}) }}
                            </div>
                        </div>

                        <div class="relative inline-block transition-transform duration-300 hover:scale-105 group w-full ">
                            <button type="submit" class="relative z-10 w-full flex justify-center py-3 px-5 rounded-md text-md font-semibold text-white bg-main-green transition-transform duration-300">
                                {{ 'registration_page.sign_up_button'|trans }}
                            </button>
                        </div>
                    {{ form_end(registrationForm) }}
                </div>
            </div>
        </div>
    </div>

    <script>
        // Logo switching functionality
        function updateLogo() {
            const lightLogo = document.querySelector('.light-mode-logo');
            const darkLogo = document.querySelector('.dark-mode-logo');

            if (document.documentElement.classList.contains('dark')) {
                lightLogo.classList.add('hidden');
                darkLogo.classList.remove('hidden');
            } else {
                lightLogo.classList.remove('hidden');
                darkLogo.classList.add('hidden');
            }
        }

        // Initial logo update
        updateLogo();

        // Watch for theme changes
        const observer = new MutationObserver((mutations) => {
            mutations.forEach((mutation) => {
                if (mutation.type === 'attributes' && mutation.attributeName === 'class') {
                    updateLogo();
                }
            });
        });

        observer.observe(document.documentElement, {
            attributes: true,
            attributeFilter: ['class']
        });
    </script>
{% endblock %}
